
/*
    内置类型声明文件。
    TS为JS运行时可用的所有标准化内置API都提供了类型声明文件。
    比如数组，数组的所有方法都会有相应的代码提示以及类型信息：
    在lib.es5.d.ts文件中.
*/

let arr: number[] = [1,2,3]
arr.forEach(item => item)

document.querySelector

window
